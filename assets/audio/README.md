# Background music

`wedding-music.wav` is a short, original placeholder melody used to verify the audio controls. Replace it with the final music file (typically `wedding-music.mp3`) and update `music.src` in `data/wedding.json`.

The template intentionally tolerates a missing file, so the rest of the invitation continues to work while music is being selected.
